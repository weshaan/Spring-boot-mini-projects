spring:
  datasource:
    url: jdbc:mysql://mysql:3306/smart_inventory
    username: smartuser
    password: smartpass

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  sql:
    init:
      mode: never

server:
  port: 8080
